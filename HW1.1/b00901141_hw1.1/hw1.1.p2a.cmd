::There are 1063 files under my home directory.

find ${HOME} -type f | wc -l

::find all files under home directory. Since one file will be in one line, so then just count the number of the lines of the output.


